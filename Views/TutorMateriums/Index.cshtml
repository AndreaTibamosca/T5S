@model IEnumerable<T5S.Models.TutorMaterium>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdMateria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTutor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTutorMateriaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdMateria)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTutor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTutorMateriaNavigation.IdMateria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTutorMateria">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTutorMateria">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTutorMateria">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
