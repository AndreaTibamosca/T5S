@model IEnumerable<T5S.Models.Calendario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaCalendario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionCalendario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaCalendario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionCalendario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCalendario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCalendario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCalendario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
